<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StockMaster"
        tools:targetApi="34">

        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.StockMaster.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.LoginActivity"
            android:exported="false"
            android:theme="@style/Theme.StockMaster.NoActionBar" />

        <activity
            android:name=".activities.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.StockMaster.NoActionBar" />

        <activity
            android:name=".activities.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.StockMaster.NoActionBar" />

        <activity
            android:name=".activities.ProductDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.StockMaster.NoActionBar" />

        <activity
            android:name=".activities.AddEditProductActivity"
            android:exported="false"
            android:theme="@style/Theme.StockMaster.NoActionBar" />

        <activity
            android:name=".activities.SaleDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.StockMaster.NoActionBar" />

        <activity
            android:name=".activities.AlertsActivity"
            android:exported="false"
            android:theme="@style/Theme.StockMaster.NoActionBar" />

    </application>

</manifest>
